Corrected a factual error in the SWTOS copy added in step 013. The COR24-TB board HAS hardware multiply; it lacks hardware DIVIDE. The step-013 descriptions said 'no hardware multiply', wording lifted from the sw-tos README's portability note and misapplied as a platform claim. Fixed all three occurrences to 'no hardware divide': the SWTOS pipeline card in components/toolchain/pipelines.rs, the demo card description in data/demos.rs, and the ToolEntry description in data/tools/data.rs. Verified the rest of the site was already correct -- data/isa/instructions.rs documents a 'mul' instruction and no 'div', and docs/language-building-tech.md already said 'no hardware divide' -- so no other files needed changing. Gate green: fmt, clippy -D warnings, 50 tests, wasm check, build-pages; sw-checklist unchanged at its 18-FAIL baseline. Merged to main with plain git and pushed; per user instruction the gh CLI was not used.