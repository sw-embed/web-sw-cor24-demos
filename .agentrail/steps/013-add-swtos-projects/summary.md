Added the SWTOS projects (sw-embed/sw-tos and sw-embed/web-sw-tos) across the whole site. SWTOS is a MINIX-inspired microkernel OS written in PL/SW, so it got its own taxonomy slot rather than being folded in with the language projects: new 'Operating System' demo category (2 cards) + filter entry, new 'Operating Systems' ToolGroup and ToolCategory::OperatingSystem, ProjectRow entries in the status table (sw-tos under a new 'Operating System' group, web-sw-tos under Web UIs), 3 new ecosystem dependency edges with a new DepGroup::OperatingSystem rendered in teal plus a 'SWTOS depends on' dep block and legend entry, and an 'Operating System (COR24-side)' pipeline group on the Toolchain page. Hero, About, and 'About COR24' copy now mention the microkernel. Both repos added to tools/gen-status REPOS and scripts/gen-status.sh, and listed in README.md and AGENTS.md. Regenerated src/data/status/generated.rs and all 15 images/*-issue-chart.svg. Also added an 'Operating System' subgraph to docs/mermaid-diagram2.mmd and re-rendered images/mermaid-diagram3.svg (which incidentally fixed literal backslash-n in every node label). Per user decision, reports/commits.html and reports/closed-issues.html were left alone -- their generator lives in the sibling sw-cor24-project repo, which is not cloned here, and its repo discovery grep excludes sw-tos/web-sw-tos. Two supporting fixes: 6 pre-existing explicit_auto_deref clippy errors in demo_grid.rs that were blocking the gate, and a split of the About narrative into components/about/intro.rs so sw-checklist stayed at its 18-FAIL baseline. Gate green: fmt, clippy -D warnings, 50 tests, wasm check, build-pages. Verified in the browser at all touched pages. Branch feat/add-swtos-projects, 3 commits, not yet pushed.